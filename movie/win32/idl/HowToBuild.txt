Start the VC command prompt and execute the following command.
MIDL IRendererBufferAccess.idl
MIDL IRendererBufferVideo.idl

Some files will be generated, so copy the _i.c and .h files to the folder above.
Originally, it is better to edit the vc project and execute the MDIL command with a custom build.
